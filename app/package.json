{
  "name": "chuan-hoa-cong-to",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "node -e \"require('fs').mkdirSync('dist',{recursive:true}); for (const item of ['index.html','app.js','styles.css','vendor','public','vercel.json']) require('fs').cpSync(item, 'dist/' + item, {recursive:true});\""
  }
}
